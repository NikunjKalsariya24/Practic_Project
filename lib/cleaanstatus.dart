import 'package:flutter/material.dart';

class CleanStatusScreen extends StatelessWidget {
  const CleanStatusScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
