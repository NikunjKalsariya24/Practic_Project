import 'package:flutter/material.dart';

class CleanSettingScreen extends StatelessWidget {
  const CleanSettingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
